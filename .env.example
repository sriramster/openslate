# Required — a random secret for JWT tokens
JWT_SECRET=change-me-to-a-random-string

# Optional — set to your domain for automatic HTTPS (Let's Encrypt)
DOMAIN=

# Optional — Cloudflare R2 credentials for media uploads
R2_BUCKET=
R2_ACCOUNT_ID=
R2_ACCESS_KEY=
R2_SECRET_KEY=
